var x=3;
var y=4;
var z=x+y;

function foo(x, y)
{
	return x+y;
}

var w=foo(5,6);

println("Z is ", z);
println("W is ", w);

var i;
for(i=0; i<10; i++)
	println("I is ", i);

var arr=[1,4,9,16,25];
arr[5]=6*6;
for(i=0; i<6; i++)
	println("arr[i] is ", arr[i]);

var obj={x:6,y:7};
obj.z=obj.x+obj.y;
println("tstobj is ", obj.x+obj.y);
println("tstobj2 is ", obj.z);
